# =============================================================================
# Web Environment Variables
# =============================================================================
# Copy to .env: cp .env.example .env

# API Server URL (must have VITE_ prefix for client access)
# Server runs on port 3008, web dev server on port 3001
VITE_SERVER_URL=http://localhost:3008
