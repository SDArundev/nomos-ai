{
  "patterns": [
    {
      "id": "VP-001",
      "name": "UUID instead of branded ID",
      "description": "Entity creation uses crypto.randomUUID() instead of generating branded IDs matching the type system (F###, P###, S###)",
      "category": "CAT-API",
      "type": "logic",
      "frequency": 2,
      "first_seen": "2026-02-06",
      "last_seen": "2026-02-06",
      "features_affected": ["F017", "F018", "F019"],
      "root_cause": "Repositories use generic UUID generation without awareness of branded type conventions",
      "prevention": "Implement sequential ID generation in base repository or ID service",
      "detection": "Check create handlers for crypto.randomUUID() vs branded format",
      "fix_pattern": "Query max(id), parse numeric suffix, increment, format with prefix"
    },
    {
      "id": "VP-002",
      "name": "State machine duplication",
      "description": "VALID_TRANSITIONS constant duplicated across server and client code (4 locations)",
      "category": "CAT-CORE",
      "type": "quality",
      "frequency": 3,
      "first_seen": "2026-02-06",
      "last_seen": "2026-02-06",
      "features_affected": ["F018", "F019", "F025", "F027"],
      "root_cause": "No shared package exports state machine logic, each module defines its own",
      "prevention": "Define state machines in @nomos-ai/types and import everywhere",
      "detection": "Search for VALID_TRANSITIONS across codebase",
      "fix_pattern": "Extract to shared package, export as const, import in consumers"
    },
    {
      "id": "VP-003",
      "name": "DB schema / Zod schema drift",
      "description": "Drizzle table schema missing columns that exist in the corresponding Zod type schema (e.g., project.status, feature.preImplemented)",
      "category": "CAT-DB",
      "type": "integration",
      "frequency": 3,
      "first_seen": "2026-02-06",
      "last_seen": "2026-02-06",
      "features_affected": ["F007", "F008"],
      "root_cause": "Zod schemas and Drizzle schemas evolved independently without cross-validation",
      "prevention": "Add build-time check comparing Zod schema keys against Drizzle column names",
      "detection": "Compare exported Zod schema shape with Drizzle table columns",
      "fix_pattern": "Add missing columns + migration, or remove from Zod schema if not needed"
    },
    {
      "id": "VP-004",
      "name": "TOCTOU in bulk operations",
      "description": "Bulk status update validates then modifies in separate steps without transaction",
      "category": "CAT-API",
      "type": "runtime",
      "frequency": 3,
      "first_seen": "2026-02-06",
      "last_seen": "2026-02-06",
      "features_affected": ["F018"],
      "root_cause": "Validation and mutation not wrapped in same transaction",
      "prevention": "Always use transactions for validate-then-mutate patterns",
      "detection": "Look for await Promise.all() followed by separate update calls",
      "fix_pattern": "Wrap in Drizzle transaction: db.transaction(async (tx) => { ... })"
    },
    {
      "id": "VP-005",
      "name": "Error handling boilerplate duplication",
      "description": "Identical try-catch error handling pattern repeated 32+ times across all oRPC router handlers",
      "category": "CAT-API",
      "type": "quality",
      "frequency": 3,
      "first_seen": "2026-02-06",
      "last_seen": "2026-02-06",
      "features_affected": ["F017", "F018", "F019"],
      "root_cause": "No shared error handling middleware or utility",
      "prevention": "Create withErrorHandling wrapper or oRPC middleware",
      "detection": "Search for 'ORPCError' + 'NOT_FOUND' pattern across routers",
      "fix_pattern": "Extract to shared utility function in packages/api/src/utils/"
    },
    {
      "id": "VP-006",
      "name": "Missing ownership/access control (IDOR)",
      "description": "Entity tables have no userId column — any authenticated user can CRUD any entity",
      "category": "CAT-DB",
      "type": "security",
      "frequency": 2,
      "first_seen": "2026-02-06",
      "last_seen": "2026-02-06",
      "features_affected": ["F007", "F008", "F009", "F012", "F013"],
      "root_cause": "Tables designed without multi-tenancy/ownership model",
      "prevention": "Always include userId column with FK to auth.user table in entity schemas",
      "detection": "Check entity tables for userId/ownerId columns",
      "fix_pattern": "Add userId column, update repository methods to filter by userId, pass from auth context"
    },
    {
      "id": "VP-007",
      "name": "Missing security headers",
      "description": "Hono server missing X-Frame-Options, CSP, HSTS, X-Content-Type-Options headers",
      "category": "CAT-API",
      "type": "security",
      "frequency": 2,
      "first_seen": "2026-02-06",
      "last_seen": "2026-02-06",
      "features_affected": ["F015"],
      "root_cause": "Security middleware not configured in server setup",
      "prevention": "Add secureHeaders middleware to all Hono servers",
      "detection": "Check server index.ts for secureHeaders or equivalent middleware",
      "fix_pattern": "import { secureHeaders } from 'hono/secure-headers'; app.use(secureHeaders())"
    },
    {
      "id": "VP-008",
      "name": "Feature status mismatch (phantom in_progress)",
      "description": "Features marked as in_progress in features.json but zero implementation code exists",
      "category": "CAT-CORE",
      "type": "integration",
      "frequency": 2,
      "first_seen": "2026-02-06",
      "last_seen": "2026-02-06",
      "features_affected": ["F031", "F032", "F033", "F034"],
      "root_cause": "Status was updated prematurely before implementation started",
      "prevention": "Only transition to in_progress after initial code is committed",
      "detection": "Cross-reference feature status with git log/file existence",
      "fix_pattern": "Reset status to pending if no implementation files exist"
    },
    {
      "id": "VP-009",
      "name": "Hardcoded frontend constants",
      "description": "CATEGORIES, PHASES, SIZES, statusColors defined inline in components instead of imported from types",
      "category": "CAT-UI",
      "type": "quality",
      "frequency": 2,
      "first_seen": "2026-02-06",
      "last_seen": "2026-02-06",
      "features_affected": ["F025", "F026", "F027", "F028", "F030"],
      "root_cause": "Components developed independently without centralizing shared constants",
      "prevention": "Export all enum values and display metadata from @nomos-ai/types",
      "detection": "Search for inline const arrays matching type enum values",
      "fix_pattern": "Export from types, import in all consuming components"
    },
    {
      "id": "VP-010",
      "name": "Non-atomic ID generation race condition",
      "description": "getNextId() reads MAX(id), parses, increments, returns — not atomic. Concurrent creates produce duplicate IDs",
      "category": "CAT-API",
      "type": "runtime",
      "frequency": 1,
      "first_seen": "2026-02-06",
      "last_seen": "2026-02-06",
      "features_affected": ["F017", "F018", "F019"],
      "root_cause": "SELECT MAX + increment pattern without transaction or lock",
      "prevention": "Always wrap read-increment-write in database transaction",
      "detection": "Look for MAX(id) queries followed by non-transactional returns",
      "fix_pattern": "Wrap in db.transaction(), or use sequence table, or use RETURNING with INSERT"
    },
    {
      "id": "VP-011",
      "name": "Type-unsafe Record<string, unknown> in update handlers",
      "description": "Router update handlers construct dynamic update objects using Record<string, unknown>, losing all Zod type safety",
      "category": "CAT-API",
      "type": "quality",
      "frequency": 1,
      "first_seen": "2026-02-06",
      "last_seen": "2026-02-06",
      "features_affected": ["F017", "F018", "F019"],
      "root_cause": "Dynamic object construction without type-aware filtering",
      "prevention": "Use Object.fromEntries with proper typing or typed helper utility",
      "detection": "Search for 'Record<string, unknown>' in router files",
      "fix_pattern": "Replace with: const data = Object.fromEntries(Object.entries(input.data).filter(([,v]) => v !== undefined)) as Partial<typeof input.data>"
    }
  ]
}
